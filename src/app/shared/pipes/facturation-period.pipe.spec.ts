import { FacturationPeriodPipe } from './facturation-period.pipe';

describe('FacturationPeriodPipe', () => {
  it('create an instance', () => {
    const pipe = new FacturationPeriodPipe();
    expect(pipe).toBeTruthy();
  });
});
